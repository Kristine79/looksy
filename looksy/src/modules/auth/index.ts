// Auth module — public API
// Authentication is handled by Clerk. This module provides helpers
// and re-exports used across the application.

export type { UserAuth } from "./types";

// Placeholder until Clerk integration is added
export const AUTH_MODULE = true;
